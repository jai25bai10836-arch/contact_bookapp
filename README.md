# 📚 Simple Python Contact Book Application

This is a command-line interface (CLI) application for managing contacts, built using Python. It allows users to add, view, search, and delete contacts stored in a simple in-memory dictionary.

## ✨ Features

* **Add Contact:** Store a person's name, phone number, and an optional email address.
* **View Contacts:** Display a list of all saved contacts.
* **Search Contact:** Find a specific contact by name.
* **Delete Contact:** Remove a contact from the book by name.
* **Persistent Menu:** The application runs in a loop, providing continuous access to all functions until the user chooses to exit.

## 💻 Installation and Setup

Since this is a simple, single-file application with no external dependencies, setup is minimal.

1.  **Clone the repository (or download the file):**
    ```bash
    git clone [your-repository-url]
    cd [your-repository-name]
    ```

2.  **Ensure you have Python installed:**
    This application is compatible with **Python 3**. You can check your version:
    ```bash
    python --version
    # or
    python3 --version
    ```

## 🚀 How to Run the Application

Execute the Python script directly from your terminal:

```bash
python contact_book.py
# (Assuming the file is named contact_book.py)# SIMPLE-CONTACT_BOOK
